import { Card } from "../ui/Card";

export function FeedbackPanel({ feedback, loading, error }: any) {
  if (loading) return (<Card className="flex flex-col items-center justify-center py-12"><div className="w-8 h-8 border-4 border-accent-cyan border-t-transparent rounded-full animate-spin mb-3"></div><span className="text-muted font-mono uppercase">Analyzing your essay...</span></Card>);
  if (error) return (<Card className="border-danger/50"><div className="text-danger font-mono text-sm p-4">{error}</div></Card>);
  if (!feedback) return (<Card><h3 className="text-xl font-bold text-accent-purple mb-4 font-sans uppercase">AI Feedback</h3><p className="text-muted text-sm mb-4">Submit your essay to receive IELTS feedback.</p><ul className="list-disc pl-5 text-muted text-sm font-mono"><li>IELTS Band Score (0-9)</li><li>Grammar feedback</li><li>Vocabulary suggestions</li><li>Improved version</li></ul></Card>);
  return (<Card><h3 className="text-xl font-bold text-accent-purple mb-4 font-sans uppercase">AI Feedback</h3><div className="flex justify-between items-center py-3 border-b border-border"><span className="font-mono uppercase text-sm">IELTS Band Score</span><strong className="text-3xl text-accent-cyan">{feedback.score}</strong></div><div className="py-3 border-b border-border"><h4 className="font-mono uppercase text-sm font-semibold mb-1">Grammar & Mechanics</h4><p className="text-muted text-sm">{feedback.grammar_feedback}</p></div><div className="py-3 border-b border-border"><h4 className="font-mono uppercase text-sm font-semibold mb-1">Vocabulary</h4><p className="text-muted text-sm">{feedback.vocabulary_feedback}</p></div><div className="pt-3"><h4 className="font-mono uppercase text-sm font-semibold mb-1">Improved Version</h4><pre className="text-xs bg-background p-3 rounded border border-border text-accent-cyan whitespace-pre-wrap font-mono max-h-60 overflow-auto">{feedback.improved_version}</pre></div></Card>);
}